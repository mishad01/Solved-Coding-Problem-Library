#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
int main() {
  int n,m; cin >> n>>m;
  int a[n];
  map<int,int>mp;
  for (int i = 0; i <= n; i++) {
    cin >> a[i];
    mp[a[i]]++;
  }
  for(int i = 1;i<=m;i++){
    cout<<mp[i]<<nl;
  }

  
}
10