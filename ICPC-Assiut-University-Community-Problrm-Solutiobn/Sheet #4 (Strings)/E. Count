#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
int main(){
	string a;
	cin>>a;
	int sum = 0;
	for(int i = 0;i<a.size();i++){
		if(a[i]>47 && a[i]<58){
			sum = sum + (a[i]-48);
		}
	}
	cout<<sum;
	
}