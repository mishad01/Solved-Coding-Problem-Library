#include<bits/stdc++.h>
using namespace std;
#define l endl;
int main ()
{
	/*cout<<max(3,9)<<l;
	cout<<max({89,17,69,56,1001})<<l;
	cout<<min(3,9)<<l;
	cout<<min({7,89,34,12,11})<<l;
	cout<<abs(-69)<<l;
	cout<<sqrt(64)<<l;
	cout<<cbrt(64)<<l;
	cout<<pow(2,5)<<l;
	cout<<log2(16)<<l;
	  int a[10];
  memset(a, -1, sizeof a);
  for (int i = 0; i < 10; i++) {
    cout << a[i] << ' ';
  }
  int a[10];
  
  //To find ASCII VALUE
  char x='a',y = '9';
  cout<<(int)('1');

  //To find if character or disigit
  char x='a',y = '9';
  cout<<isdigit(y);

  //To find if character big or small
  char x='a',y = 'A';
  cout<<char(tolower(y))<<l;
  cout<<char(toupper(x))<<l
  transform (a.begin(),a.end(),a.begin(), ::tolower);
  cout<<a;

  //swap

  int a =20,b=40;
  swap(a,b);
  cout<<a << " "<<b;

  //To find GCD
  int a =12,b=16;
  cout<<__gcd(a,b)<<l;

  //To find GRandom number
  cout<<rand()<<l;

  //to check wheteher + - right or not
  cerr<<l;

  // to get all the value same in array 
  fill(a+0,a+10, 23);
  for (int i = 0; i < 10; i++) {
    cout << a[i] << ' ';

    // to reverse

    int a[5]={1,2,3,4,5};
    reverse(a,a+5)
    for(int i = 0;i<n;i++);
    {
      cout<<a[i];
    }

    // to sort
    sort(a,a+n)
    for(int i = 0;i<n;i++);
    {
      cout<<a[i];
    }

    //to find three max value

    cout<<max(x,max(y,max(z,t)));


    */
  //to find if the equation right or not in the middle of a code
  cerr<<z;
  }

}