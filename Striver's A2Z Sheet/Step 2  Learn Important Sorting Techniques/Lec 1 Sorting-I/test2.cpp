#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
bool isLiked(int number) {
    return number % 3 != 0 && number % 10 != 3;
}
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--) {
        int count = 1;
        int number = 0;
        
    }
    return 0;
}