#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define nl '\n'
#define PI 3.1416
#define ADD(a,b) (a+b) //We can also define function using macros
#define square(x,y) x*y
int32_main() { // notice that we are using int32_t instead of int here because int is redefined as long long
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int a = 5 //Here int is Long long as we wrote int32_main
    //cout<<PI;
    int r = 3;
    cout<<3.1416 * r<<nl;
    cout<<ADD(5,5)<<nl;
    cout<<square(5,5);
}