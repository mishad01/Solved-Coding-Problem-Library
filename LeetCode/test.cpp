#include <iostream>
#include <vector>
#include <string>

int main() {
    std::vector<std::string> words = {"abc", "car", "ada", "racecar", "cool"};
    
    for(auto x : words) {
        std::cout << x[i] << std::endl;
    }
    
    return 0;
}
